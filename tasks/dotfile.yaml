- name: Installing stow
  become: true
  apt: name=stow
  tags:
    - install
    - dotfiles
    - stow


- name: Cloning .dotfiles
  become: true
  become_user: jyorko
  ansible.builtin.git:
    repo: 'https://github.com/joshyorko/.dotfiles.git'
    dest: "/home/jyorko/.dotfiles"
    recursive: yes
    update: yes
    force: no
  tags:
    - install
    - dotfiles
    - stow

- name: Stow dotfiles
  become: true
  become_user: jyorko
  ansible.builtin.shell: cd $HOME/.dotfiles
  tags:
    - install
    - dotfiles
    - stow
