- name: Work Repo Exists!
  stat:
    path: "{{ work }}/repos"
  register: repos
  
- name: Work - work
  ansible.builtin.git:
    repo: 'git@github.com:mygainwell/maui-data-integration.git'
    dest: "{{ repos }}/maui-data-integration"
    accept_hostkey: yes
  tags:
    - work

- name: Work - SASI
  ansible.builtin.git:
    repo: 'git@github.com:mygainwell/SASI-Project.git'
    dest: "{{ repos }}/SASI-Project"
    accept_hostkey: yes
  tags: 
    - work

- name: Work - IronRipper3K
  ansible.builtin.git:
    repo: 'git@github.com:mygainwell/IronRipper3K.git'
    dest: "{{ repos }}/IronRipper3K"
    accept_hostkey:
  tags:
    - work


